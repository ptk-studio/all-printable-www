# all-printable — journal

Newest first. One entry per session that changed something.

---

## 2026-09-04 · framework session — this repo became its own product

Until today this repo was half of the `all-printable` project, which spanned it and
`all-printable-app-www`. A project now owns exactly one repo, so it became two projects, and
this directory is this one's own record.

**Learned:** the shared project had recorded almost nothing about this repo — 3 mentions on the
board against 167 for the app. Two repos under one project does not split attention evenly; it
lets the quieter one go unwritten while the record still looks complete.

**Left for next time:** a declared metric, a verification section that has actually been run,
and a first look at whether the ~40 redirect stubs still resolve.
